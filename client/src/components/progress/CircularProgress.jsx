import React from 'react';
import { CircularProgress as CommonCircularProgress } from '../common/CircularProgress.jsx';

export const CircularProgress = (props) => <CommonCircularProgress {...props} />;
