import React from 'react';
import { DifficultyFilter } from '@components/filters/DifficultyFilter.jsx';

export const TestFilters = ({ value, onChange }) => <DifficultyFilter value={value} onChange={onChange} />;
