import React from 'react';
import AppRoutes from '@routes/AppRoutes.jsx';

/**
 * routes.jsx — thin wrapper that mounts the full route tree.
 * All route definitions live in src/routes/AppRoutes.jsx.
 */
const Routes = () => <AppRoutes />;

export default Routes;
