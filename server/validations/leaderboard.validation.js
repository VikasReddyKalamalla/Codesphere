// ─── Leaderboard Validation Rules ────────────────────────────────────────────

const getLeaderboardRules = {
  // Query params only — no body validation needed
};

module.exports = { getLeaderboardRules };
