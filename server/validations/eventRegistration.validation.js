// ─── Event Registration Validation Rules ──────────────────────────────────────

const registerForEventRules = {
  // No body required — eventId from params, userId from auth token
};

module.exports = { registerForEventRules };
