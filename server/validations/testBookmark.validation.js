// ─── Test Bookmark Validation Rules ──────────────────────────────────────────

const addBookmarkRules = {
  // No body required — testId from params, userId from auth token
};

module.exports = { addBookmarkRules };
