// ─── Payment Validation Rules ─────────────────────────────────────────────────
// Payments are auto-generated as mock records — no user-facing creation rules

const getPaymentRules = {
  // Query params only
};

module.exports = { getPaymentRules };
