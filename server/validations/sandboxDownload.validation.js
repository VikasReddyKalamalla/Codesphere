// ─── Sandbox Download Validation Rules ───────────────────────────────────────

const recordDownloadRules = {
  // No body required — templateId from params, userId from auth token
};

module.exports = { recordDownloadRules };
