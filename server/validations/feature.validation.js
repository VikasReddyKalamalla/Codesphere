// ─── Feature Access Validation Rules ─────────────────────────────────────────

const checkAccessRules = {
  // Query param: feature (string, required)
  // e.g. GET /api/features/access?feature=codexAccess
};

module.exports = { checkAccessRules };
