// ─── Event Bookmark Validation Rules ─────────────────────────────────────────

const addBookmarkRules = {
  // No body required — eventId from params, userId from auth token
};

module.exports = { addBookmarkRules };
