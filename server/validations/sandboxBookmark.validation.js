// ─── Sandbox Bookmark Validation Rules ───────────────────────────────────────

const addBookmarkRules = {
  // No body required — projectId from params, userId from auth token
};

module.exports = { addBookmarkRules };
