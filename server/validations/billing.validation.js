// ─── Billing Validation Rules ─────────────────────────────────────────────────
// Billing records are auto-generated — no user-facing creation rules needed

const getBillingHistoryRules = {
  // Query params only
};

module.exports = { getBillingHistoryRules };
